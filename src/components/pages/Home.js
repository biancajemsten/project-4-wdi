import React from 'react';

const Home = () => {
  return(
    <section className="hero is-bold is-fullheight">
      <div className="hero-body">
        <div className="container">
          <h1 className="title">EVENTS, EVENTS AND MORE EVENTS</h1>
          <h2 className="subtitle">Coordination is key</h2>
        </div>
      </div>
    </section>
  );
};

export default Home;
